name: Deploy Production

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  deploy-production:
    name: Build & Deploy to Production
    runs-on: ubuntu-latest
    environment: production  # requires manual approval in repo settings
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Build (OpenNext + Cloudflare)
        run: npx opennextjs-cloudflare build

      - name: Deploy to production
        run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}

      - name: Set BACKEND_API_KEY on production Worker
        run: |
          OUTPUT_SECRET=$(echo "$BACKEND_API_KEY" | npx wrangler secret put BACKEND_API_KEY 2>&1) || { echo "$OUTPUT_SECRET"; exit 1; }
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          BACKEND_API_KEY: ${{ secrets.BACKEND_API_KEY }}

      - name: Smoke test
        run: |
          echo "Waiting 10s for propagation..."
          sleep 10
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://lorenzomaiuri.dev/)
          if [ "$STATUS" != "200" ]; then
            echo "Smoke test failed: HTTP $STATUS"
            exit 1
          fi
          echo "Smoke test passed: HTTP $STATUS"
